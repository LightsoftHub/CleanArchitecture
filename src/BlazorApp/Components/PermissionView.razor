@if (hasPermission)
{
    @ChildContent
}

@code {
    [Parameter]
    public RenderFragment ChildContent { get; set; } = default!;

    [Parameter]
    public string? Permission { get; set; } = default!;

    private bool hasPermission => string.IsNullOrEmpty(Permission) || CurrentUser.HasPermission(Permission);
}